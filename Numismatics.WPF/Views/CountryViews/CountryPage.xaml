<Page x:Class="Numismatics.WPF.View.CountryView.CountryPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Numismatics.WPF.View.CountryView"
      mc:Ignorable="d" 
      d:DesignHeight="600" d:DesignWidth="1000"
      Background="{StaticResource BrushBg}"
      Title="Country page">

    <Grid Style="{StaticResource GridContainderStyle}" Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0">
                <Label Content="Countries page" Style="{StaticResource TitleStyle}" Padding="0"/>
                <Label Content="{Binding CountryDisplayViewModel.TotalCountriesText, UpdateSourceTrigger=PropertyChanged}"
                       Style="{StaticResource LabelTitleStyle}" Padding="2,0,0,0"/>
            </StackPanel>

            <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                <Button Content="+ Add" Style="{StaticResource CrudButtonStyle}" Width="110"
                        Command="{Binding CountryDisplayViewModel.AddCountryCommand}"/>
                <Button Content="Update" Style="{StaticResource SaveButtonStyle}" Width="110" Height="42"
                        Command="{Binding CountryDisplayViewModel.UpdateCountryCommand}"/>
                <Button Content="Delete" Style="{StaticResource ExitButtonStyle}" Width="110" Height="42"
                        Command="{Binding CountryDisplayViewModel.DeleteCountryCommand}"/>
            </StackPanel>
        </Grid>

        <Border Grid.Row="1" Style="{StaticResource CardOnDarkStyle}" Margin="0,20,0,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Margin="0,0,10,0">
                    <Label Content="NAME" Style="{StaticResource LabelStyleDark}"/>
                    <TextBox Style="{StaticResource TextBoxInputStyle}"
                             Text="{Binding CountryDisplayViewModel.CountrySearchDataViewModel.Name, UpdateSourceTrigger=PropertyChanged}"/>
                </StackPanel>
                <StackPanel Grid.Column="1" Margin="0,0,10,0" MaxWidth="220" HorizontalAlignment="Left">
                </StackPanel>

                <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Bottom">
                    <Button Style="{StaticResource IconButtonStyle}" Command="{Binding CountryDisplayViewModel.SearchCommand}"
                            ToolTip="Search">
                        <Image Source="/Images/SearchIcon.png" Width="16" Height="16"/>
                    </Button>
                    <Button Style="{StaticResource IconButtonStyle}" Command="{Binding CountryDisplayViewModel.RefreshSearchCommand}"
                            ToolTip="Reset filters" Margin="8,0,0,0">
                        <Image Source="/Images/RefreshIcon.png" Width="16" Height="16"/>
                    </Button>
                </StackPanel>
            </Grid>
        </Border>

        <TextBlock Grid.Row="2" Text="Collection" Style="{StaticResource CardHeaderStyle}" Margin="4,22,0,0"/>

        <DataGrid Grid.Row="3"
            Style="{StaticResource ModernDataGridStyle}"
            Margin="0,6,0,0"
            ItemsSource="{Binding CountryDisplayViewModel.CurrentCountries, UpdateSourceTrigger=PropertyChanged}"
            SelectedItem="{Binding CountryDisplayViewModel.SelectedCountry, UpdateSourceTrigger=PropertyChanged}"
            MouseDoubleClick="ShowCountry"
            HeadersVisibility="Column"
            AutoGenerateColumns="False"
            IsReadOnly="True"
            CanUserAddRows="False"
            HorizontalAlignment="Stretch"
            VerticalAlignment="Stretch">
            <DataGrid.Columns>
                <DataGridTextColumn Header="NAME" Width="2*" Binding="{Binding Name}"
                                    ElementStyle="{StaticResource DataGridTextStyle}" CanUserSort="False"></DataGridTextColumn>
                <DataGridTextColumn Header="BANK" Width="2*" Binding="{Binding Bank}"
                                    ElementStyle="{StaticResource DataGridTextStyle}" CanUserSort="False"></DataGridTextColumn>
                <DataGridTextColumn Header="CAPITAL" Width="*" Binding="{Binding Capital}"
                                    ElementStyle="{StaticResource DataGridTextStyle}" CanUserSort="False"></DataGridTextColumn>
                <DataGridTextColumn Header="START YEAR" Width="0.8*" Binding="{Binding StartYear}"
                                    ElementStyle="{StaticResource DataGridTextStyle}" CanUserSort="False"></DataGridTextColumn>
                <DataGridTextColumn Header="ERA" Width="0.4*" Binding="{Binding StartYearEra}"
                                    ElementStyle="{StaticResource DataGridTextStyle}" CanUserSort="False"></DataGridTextColumn>
                <DataGridTextColumn Header="END YEAR" Width="0.8*" Binding="{Binding EndYear}"
                                    ElementStyle="{StaticResource DataGridTextStyle}" CanUserSort="False"></DataGridTextColumn>
                <DataGridTextColumn Header="ERA" Width="0.4*" Binding="{Binding EndYearEra}"
                                    ElementStyle="{StaticResource DataGridTextStyle}" CanUserSort="False"></DataGridTextColumn>
            </DataGrid.Columns>
        </DataGrid>

        <TextBlock Grid.Row="4" Text="Double-click a row to open country details" 
                   Foreground="{StaticResource BrushTextMuted}" FontSize="11" Margin="4,10,0,0"/>
    </Grid>
</Page>
